export interface Location {
  city: string;
  state: string;
  country: string;
  address: string;
}

export interface Custodian {
  address: string;
  name: string;
  phone: string;
  email: string;
}
